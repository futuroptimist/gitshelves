apiVersion: v1
kind: Service
metadata:
  name: {{ include "gitshelves.fullname" . }}
  labels:
    {{- include "gitshelves.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "gitshelves.selectorLabels" . | nindent 4 }}
  ports: [{name: http, port: {{ .Values.service.port }}, targetPort: http, protocol: TCP}]
