# Gridfinity STL outputs (2024)

This directory stores the CI-rendered `baseplate_2x6.stl` mesh for 2024.
STLs are generated from `openscad/baseplate_2x6.scad` by the build-stl workflow
and uploaded as artifacts so contributors can download the compiled baseplates
without running OpenSCAD locally.
